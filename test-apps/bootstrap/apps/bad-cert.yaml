apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: bad-tls
  namespace: default
spec:
  secretName: bad-tls-secret
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - test.gcp.plural.sh
