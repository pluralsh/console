{
    "type": "object",
    "properties": {
        "name": {
            "type": "string",
            "description": "the name of the entity in the knowledge graph to delete"
        }
    },
    "required": ["name"]
}