{
    "type": "object",
    "properties": {
        "query": {
            "type": "string",
            "description": "A search string to query for pr file contents using semantic vector search. This should ideally target actual code functionality."
        }
    },
    "required": ["query"]
}