{
    "type": "object",
    "properties": {
        "required": {
            "type": "boolean",
            "description": "Whether a seasoned kubernetes expert would just that container logs are necessary to diagnose the given issues"
        }
    },
    "required": ["required"],
    "additionalProperties": false
}