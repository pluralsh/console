{
    "type": "object",
    "properties": {
        "required": {
            "type": "boolean",
            "description": "Whether a seasoned kubernetes expert would think that container logs are necessary to diagnose the given issues"
        }
    },
    "required": ["required"]
}