{
    "type": "object",
    "properties": {
        "query": {
            "type": "string",
            "description": "A search string to query for alert resolutions using semantic vector search."
        }
    },
    "required": ["query"],
    "additionalProperties": false
}