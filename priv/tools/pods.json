{
    "type": "object",
    "properties": {
        "service": {
            "type": "string",
            "description": "The name of the service to list pods for"
        },
        "cluster": {
            "type": "string",
            "description": "The cluster handle the service is deployed to"
        }
    },
    "required": ["service", "cluster"],
    "additionalProperties": false
}