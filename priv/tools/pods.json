{
    "type": "object",
    "properties": {
        "service_deployment": {
            "type": "string",
            "description": "The name of the Plural service to list pods for (this should not be confused with a kubernetes service object)"
        },
        "cluster": {
            "type": "string",
            "description": "The Plural cluster handle the service is deployed to"
        }
    },
    "required": ["service", "cluster"],
    "additionalProperties": false
}