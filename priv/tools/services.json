{
    "type": "object",
    "properties": {
        "query": {
            "type": "string",
            "description": "a string to query for plural service deployments by name"
        },
        "cluster": {
            "type": "string",
            "description": "the cluster handle to search for services in"
        }
    },
    "required": [],
    "additionalProperties": false
}