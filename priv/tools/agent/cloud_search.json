{
    "type": "object",
    "properties": {
        "query": {
            "type": "string",
            "description": "The query to search for.  This should be a natural language query that describes the cloud resource you are looking for."
        }
    },
    "required": ["query"],
    "additionalProperties": false
}