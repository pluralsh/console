{
    "type": "object",
    "properties": {
        "group": {
            "type": "string",
            "description": "The name of the kubernetes group to fetch the api spec for"
        },
        "version": {
            "type": "string",
            "description": "The version of the kubernetes group to fetch the api spec for"
        },
        "query": {
            "type": "string",
            "description": "a search query to use to find the relevant kind within that api group and version"
        }
    },
    "required": ["group", "version", "query"]
}