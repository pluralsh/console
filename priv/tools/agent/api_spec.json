{
    "type": "object",
    "properties": {
        "group": {
            "type": "string",
            "description": "The name of the kubernetes group to fetch the api spec for"
        },
        "version": {
            "type": "string",
            "description": "The version of the kubernetes group to fetch the api spec for"
        }
    },
    "required": ["group", "version"],
    "additionalProperties": false
}