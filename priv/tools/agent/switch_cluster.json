{
    "type": "object",
    "properties": {
        "cluster": {
            "type": "string",
            "description": "The handle of the cluster to switch to."
        }
    },
    "required": ["cluster"],
    "additionalProperties": false
}