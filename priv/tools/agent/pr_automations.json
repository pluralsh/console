{
    "type": "object",
    "properties": {
        "catalog": {
            "type": "string",
            "description": "The name of the catalog to fetch the pr automations from.  Use this to find a pr automation to actually provision some infrastructure."
        }
    },
    "required": ["catalog"],
    "additionalProperties": false
}