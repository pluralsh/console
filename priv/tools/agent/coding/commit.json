{
    "type": "object",
    "properties": {
        "repo_url": {
            "type": "string",
            "description": "The HTTPS url for the git repository you'll create a PR in"
        },
        "prompt": {
            "type": "string",
            "description": "The prompt describing the changes you want to make to the repository.  This should be a detailed description sufficient for the agent implementing them to act with a high degree of accuracy."
        }
    },
    "required": ["repo_url", "prompt"]
}