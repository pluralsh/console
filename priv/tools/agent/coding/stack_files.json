{
    "type": "object",
    "properties": {
        "stack_id": {
            "type": "string",
            "description": "The id of the stack to get the files for, this is a UUID and not a name"
        }
    },
    "required": ["stack_id"]
}