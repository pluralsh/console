{
    "type": "object",
    "properties": {
        "query": {
            "type": "string",
            "description": "A valid SQL query using the schema from the cloud_schema tool to query the cloud accounts configuration data"
        }
    },
    "required": ["query"],
    "additionalProperties": false
}