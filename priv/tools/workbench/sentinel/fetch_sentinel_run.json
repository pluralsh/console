{
    "type": "object",
    "properties": {
        "sentinel_run_id": {
            "type": "string",
            "description": "UUID of the sentinel run to fetch"
        }
    },
    "required": ["sentinel_run_id"]
}
