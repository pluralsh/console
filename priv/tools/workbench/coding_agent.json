{
    "type": "object",
    "properties": {
        "mode": {
            "type": "string",
            "enum": ["analyze", "write"],
            "description": "The mode of the coding agent (analyze or write).  If you just want to gather information about a repository, use analyze. If you want to generate a pr, use write."
        },
        "repository": {
            "type": "string",
            "description": "The url of the repository to create a PR for"
        },
        "base_branch": {
            "type": "string",
            "description": "**OPTIONAL** branch to clone for a normal run and use as its pull request base. Leave it blank unless explicitly prompted to set it. Do not use this for follow-up runs."
        },
        "head_branch": {
            "type": "string",
            "description": "Required when `followup` is true. The existing pull request's head branch. The harness checks out this branch before starting the agent so it can commit and push its additional work there."
        },
        "prompt": {
            "type": "string", 
            "description": "The prompt to give to the coding agent.  This should be a detailed prompt that describes the changes you want to make to the repository.  Commonmark Markdown formatting is encouraged to convey structure and semantic importance."
        },
        "babysit": {
            "type": "boolean",
            "description": "When true, requests babysitting behavior for this run. Enables the agent to track CI failures and user comments.  Useful for application code prs or complex changes"
        },
        "followup": {
            "type": "boolean",
            "description": "When true, this run performs additional work on an existing pull request. Set `head_branch` to that pull request's head branch. The agent commits and pushes to the current branch; it does not create a branch or a new pull request."
        }
    },
    "required": ["mode", "repository", "prompt"]
}