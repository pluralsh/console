{
    "type": "object",
    "properties": {
        "service_id": {
            "type": "string",
            "description": "UUID of the Plural service to list vulnerability reports for (from plrl_cluster_services or the API)"
        }
    },
    "required": ["service_id"]
}
