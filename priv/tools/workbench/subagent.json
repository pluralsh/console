{
    "type": "object",
    "properties": {
        "prompt": {
            "type": "string",
            "description": "The prompt to provide the subagent.  This should give all necessary context for the subagent to complete the task assigned."
        }
    },
    "required": ["prompt", "agent"]
}