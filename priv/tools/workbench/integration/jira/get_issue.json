{
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "issue_id": {
      "type": "string",
      "description": "Jira issue key or numeric ID, for example ENG-123."
    },
    "fields": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Issue fields to return. Omit for Jira's default field set."
    },
    "expand": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Jira entities to expand, such as names, schema, or changelog."
    }
  },
  "required": ["issue_id"]
}
