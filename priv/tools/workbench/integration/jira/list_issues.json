{
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "jql": {
      "type": "string",
      "description": "Jira Query Language expression used to select and order issues."
    },
    "fields": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Issue fields to return. Omit for Jira's default field set."
    },
    "cursor": {
      "type": "string",
      "description": "nextCursor from a previous response. Jira Cloud uses an opaque token; Jira Data Center uses a non-negative numeric offset."
    },
    "limit": {
      "type": "integer",
      "minimum": 1,
      "maximum": 100,
      "default": 50,
      "description": "Maximum issues to return."
    }
  },
  "required": ["jql"]
}
