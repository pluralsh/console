{
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "issue_id": {
      "type": "string",
      "description": "Jira issue key or numeric ID, for example ENG-123."
    },
    "transition_id": {
      "type": "string",
      "description": "Optional transition ID to filter the results."
    },
    "expand": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Jira entities to expand, such as transitions.fields for transition screen requirements."
    }
  },
  "required": ["issue_id"]
}
