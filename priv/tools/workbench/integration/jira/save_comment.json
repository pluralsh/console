{
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "issue_id": {
      "type": "string",
      "description": "Jira issue key or numeric ID."
    },
    "comment_id": {
      "type": "string",
      "description": "Existing comment ID to update. Omit when creating."
    },
    "body": {
      "type": "string",
      "description": "Comment text using Jira wiki markup where supported."
    },
    "visibility": {
      "type": "object",
      "additionalProperties": false,
      "description": "Optional Jira Data Center comment visibility restriction.",
      "properties": {
        "type": {
          "type": "string",
          "enum": ["role", "group"],
          "description": "Restrict the comment to a project role or group."
        },
        "value": {
          "type": "string",
          "description": "The project role or group name."
        }
      },
      "required": ["type", "value"]
    }
  },
  "required": ["issue_id", "body"]
}
