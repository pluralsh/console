{
    "type": "object",
    "properties": {
        "prompt": {
            "type": "string",
            "description": "The prompt to provide the dashboard subagent.  This should give all necessary context for the subagent to complete the task assigned and should be in markdown format to convey semantic structure.  It is also critical to include the necessary metrics and logs queries that will be used in metrics/logs graphs, which should be in prior observability agent investigations."
        }
    },
    "required": ["prompt"]
}