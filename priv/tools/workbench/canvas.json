{
    "type": "object",
    "properties": {
        "prompt": {
            "type": "string",
            "description": "The prompt to provide the dashboard subagent. This should give all necessary context for the subagent to complete the task assigned and should be in markdown format to convey semantic structure. Include the exact metrics, logs, and traces queries from prior observability investigations. When trace data is available, require the native traces visualization block instead of markdown or ASCII span diagrams."
        }
    },
    "required": ["prompt"]
}
