{
  "type": "object",
  "properties": {
    "expression": {
      "type": "string",
      "description": "Arbitrary arithmetic expression to evaluate, such as '(2 + 3) * 4 ^ 2'."
    }
  },
  "required": [
    "expression"
  ]
}
