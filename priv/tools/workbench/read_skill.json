{
    "type": "object",
    "properties": {
        "name": {
            "type": "string",
            "description": "The skill to read. You can find the list of skill names with the workbench_skills tool."
        }
    },
    "required": ["name"]
}