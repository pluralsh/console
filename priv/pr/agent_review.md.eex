This PR was generated by the <% @run.runtime.type %>/<%= @run.runtime.name %> agent runtime.  Here's some useful information you might want to know to evaluate
the ai's perfomance:

| Name        | Details |
| ----------- | ------- |
| :speech_balloon: Prompt | <%= @run.prompt %> |
| :link: Run history | [View run history](<%= Console.url("ai/agent-runs/#{@run.id}/progress") %>) |
| :memo: Todos | <%= Enum.map_join(@run.todos || [], " ", fn todo -> (if todo.done, do: "☑️ #{todo.title}", else: "☐ #{todo.title}") end) %> |
