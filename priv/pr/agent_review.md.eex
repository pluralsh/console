This PR was generated by the <%= @run.runtime.name %> (engine: <%= @run.runtime.type %>) agent runtime.  Here's some useful information you might want to know to evaluate
the ai's perfomance:

| Name        | Details |
| ----------- | ------- |
| :speech_balloon: Prompt | <%= Console.Deployments.Agents.sanitize_prompt(@run.prompt) %> |
| :link: Run history | [View run history](<%= Console.url("/ai/agent-runs/#{@run.id}") %>) |
| :memo: Todos | <%= Enum.map_join(@run.todos || [], " ", fn todo -> (if todo.done, do: "☑️ #{todo.title}", else: "☐ #{todo.title}") end) %> |
