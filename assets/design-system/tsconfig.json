{
  "extends": "../tsconfig.app.json",
  "compilerOptions": {
    "composite": true,
    "baseUrl": ".",
    "rootDir": "./src",
    "outDir": "dist",

    // TODO: get rid of all these once errors are fixed
    "strictNullChecks": false,
    "strictPropertyInitialization": false
  },
  "include": ["src/**/*"]
}
