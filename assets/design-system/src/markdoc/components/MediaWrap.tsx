import styled from 'styled-components'

export const MediaWrap = styled.div(({ theme }) => ({
  display: 'block',
  width: '100%',
  padding: theme.spacing.large,
  margin: 0,
  marginTop: theme.spacing.large,
  marginBottom: theme.spacing.large,
}))
