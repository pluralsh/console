query Namespaces {
  handleGetNamespaces @rest(path: "namespace") {
    listMeta {
      totalItems
    }
    namespaces {
      objectMeta @type(name: "types_ObjectMeta") {
        ...ObjectMeta
      }
    }
  }
}
