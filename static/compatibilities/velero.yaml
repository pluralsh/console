icon: https://velero.io/img/Velero.svg
git_url: https://github.com/vmware-tanzu/velero
release_url: https://github.com/vmware-tanzu/velero/releases/tag/v{vsn}
helm_repository_url: https://vmware-tanzu.github.io/helm-charts
versions:
- version: 1.17.0
  kube: ['1.35', '1.34', '1.33', '1.32', '1.31', '1.30', '1.29', '1.28', '1.27', '1.26',
    '1.25', '1.24', '1.23', '1.22', '1.21', '1.20', '1.19', '1.18']
  requirements: []
  incompatibilities: []
  summary:
    helm_changes: ''
    chart_updates: []
    features: ['Modernized fs-backup to a micro-service architecture, enabling concurrency
        control, cancel/resume, improved robustness across node-agent restarts, and
        steadier resource usage.', fs-backup now supports Windows clusters by allowing
        data mover pods to run on Windows nodes for backup/restore of Windows volumes.,
      Adds support for Kubernetes Volume Group Snapshots (beta) for both CSI snapshot
        backups and CSI snapshot data movement to improve write-order consistency
        across multiple volumes., 'Adds PriorityClass support across Velero components
        (server, node-agent, data movers, and repository maintenance jobs) for better
        scheduling control.', 'Improves scalability/resiliency of data movers: new
        PrepareQueueLength to avoid excessive pending pods; data movements can resume
        after node-agent restart and handle orphaned moves; restore node-selection
        and per-storage-class node selection for CSI snapshot data movement.', 'Resource
        policies gain includeExcludePolicy so include/exclude filters can be reused
        across backups, alongside existing volumePolicy.', 'Operational improvements:
        BSL availability gauge metric and BSL status checks during backup/restore;
        imagePullSecrets inheritance for data movers/maintenance jobs; parameterized
        kubelet mount path; better hook tracking; s390x binary support.']
    breaking_changes: ['Restic upload path is effectively removed for new backups:
        --uploader-type=restic is no longer valid in install config. Restores from
        existing restic-path backups are still supported until v1.19.', 'Repository
        maintenance job configs are removed from Velero server flags and must be configured
        via a repository maintenance job ConfigMap (flags removed: --keep-latest-maintenance-jobs,
        --maintenance-job-*-request/limit).', 'PVC restore behavior change: selected-node
        annotation is always removed when no node mapping exists; previously it could
        be preserved if the node existed.']
  chart_version: 11.3.2
  images: ['docker.io/bitnamilegacy/kubectl:1.35', 'velero/velero:v1.17.1']
- version: 1.17.0
  kube: ['1.35', '1.34', '1.33', '1.32', '1.31', '1.30', '1.29', '1.28', '1.27', '1.26',
    '1.25', '1.24', '1.23', '1.22', '1.21', '1.20', '1.19', '1.18']
  requirements: []
  incompatibilities: []
  summary:
    helm_changes: ''
    chart_updates: ['No Helm chart changelog/values were provided in the notes you
        pasted, so chart-specific changes (values, manifests, hooks) cannot be authoritatively
        summarized from this input.', 'For this upgrade, focus on the application-level
        changes (Velero flags/configmaps/behavior) and then validate against the Velero
        Helm chart release notes for the chart version you plan to bump to (chart
        may rename values, change defaults, or introduce new templates for node-agent/data
        movers/maintenance configmaps).']
    features: ['Modernized fs-backup to a micro-service architecture with concurrency
        control, cancel/resume, better isolation, and improved resilience to node-agent
        restarts.', 'fs-backup now supports Windows workloads end-to-end (data mover
        pods can run on Windows nodes), complementing the Windows CSI snapshot data
        movement added in 1.16.', 'Beta support for Kubernetes Volume Group Snapshots
        (VGS) for CSI snapshot backups and CSI snapshot data movement, enabling crash-consistent
        point-in-time snapshots across multiple volumes.', 'PriorityClassName support
        across Velero components (server, node-agent, data mover pods, and maintenance
        jobs) to influence scheduling priority.', 'Scalability/resiliency improvements
        for data movers: throttled creation via PrepareQueueLength, improved restart/orphan
        handling, and restore node-selection plus per-storage-class node-selection
        capabilities.', Resource policies gain reusable include/exclude filters (includeExcludePolicy)
        in addition to volumePolicy., 'Operational improvements: BSL availability
        gauge metric, imagePullSecrets inheritance for VGDP pods/maintenance jobs,
        parameter validation for install/server configmap, and better logging for
        backends.']
    breaking_changes: ["Restic uploader removal: `--uploader-type=restic` is no longer\
        \ valid in v1.17; you can\u2019t create new restic-path backups, though restores\
        \ from existing restic backups remain supported until v1.19.", 'Repository
        maintenance job settings are removed from Velero server flags and moved to
        a maintenance-job ConfigMap; the server flags `--keep-latest-maintenance-jobs`,
        `--maintenance-job-{cpu,mem}-{request,limit}` are removed.', 'PVC restore
        behavior change: Velero now always removes the `selected-node` annotation
        when no node mapping exists (previously it might be preserved if the node
        existed), which can affect scheduling/binding expectations in some restores.']
  chart_version: 11.3.2
  images: ['docker.io/bitnamilegacy/kubectl:1.35', 'velero/velero:v1.17.1']
- version: 1.16.0
  kube: ['1.35', '1.34', '1.33', '1.32', '1.31', '1.30', '1.29', '1.28', '1.27', '1.26',
    '1.25', '1.24', '1.23', '1.22', '1.21', '1.20', '1.19', '1.18']
  requirements: []
  incompatibilities: []
  summary:
    helm_changes: ''
    chart_updates: []
    features: ['Windows cluster support: Velero components (node-agent, data mover
        pods, maintenance jobs) can run on Windows nodes and can back up/restore Windows
        workloads; hybrid multi-arch/all-in-one images are available.', 'Parallel
        ItemBlock backup: item blocks can now be processed in parallel to increase
        backup throughput; controlled by the new `--item-block-worker-count` server
        flag (default 1).', 'Data mover restore scalability: for WaitForFirstConsumer
        volumes, restores can be spread across nodes by setting `ignoreDelayBinding`
        in node-agent configuration.', 'Improved data mover observability: more status/error
        logging for intermediate objects and cleanup failures, enabled by default
        in node-agent logs.', 'CSI snapshot usability improvement: retained `VolumeSnapshotContent`
        objects are no longer included in backups, reducing unnecessary CSI objects
        synced/restored.', 'BackupRepository maintenance improvements: `RecentMaintenance`
        history in BackupRepository CR, maintenance jobs are recaptured after server
        restart, and readOnly BSLs no longer initialize/run maintenance; maintenance
        interval is configurable via `fullMaintenanceInterval` (normalGC/fastGC/eagerGC).',
      'Volume Policy enhancement: can now filter volumes by PVC labels.', 'Per-object
        resource status restore: control restoring status per object via `velero.io/restore-status`
        annotation.', 'Restore helper merged: `velero`, `velero-helper`, and `velero-restore-helper`
        binaries are now included in the single Velero image.']
    breaking_changes: ['Windows support has limitations: fs-backup is not supported
        for Windows workloads, and Security Descriptors/NTFS extended attributes are
        not backed up/restored; non-admin privilege workloads may not be supported
        as a result.']
  chart_version: 10.1.3
  images: ['docker.io/bitnamilegacy/kubectl:1.35', 'velero/velero:v1.16.2']
- version: 1.15.0
  kube: ['1.35', '1.34', '1.33', '1.32', '1.31', '1.30', '1.29', '1.28', '1.27', '1.26',
    '1.25', '1.24', '1.23', '1.22', '1.21', '1.20', '1.19', '1.18']
  requirements: []
  incompatibilities: []
  summary:
    helm_changes: ''
    chart_updates: []
    features: ['Data mover micro service moves CSI snapshot data movement from node-agent
        pods to dedicated per-activity backup/restore pods, improving security (no
        hostPath), resource control, and isolation/resilience.', Item Block concepts
        and the ItemBlockAction (IBA) plugin type were introduced to group related
        resources for future concurrent backup processing; Velero ships built-in IBAs
        for Pods and PVCs., New ConfigMaps were added to tune repository maintenance
        job node placement and to configure backupPVC behavior (read-only mount and
        storageClass selection) for data mover backups., Backup repository cache size
        can now be limited per repository via a configuration ConfigMap to avoid running
        out of ephemeral storage., Performance improvements include fixing a server
        memory leak after plugin calls and inheriting client QPS/burst settings to
        plugins; Kopia maintenance memory usage is improved via upstream fixes.]
    breaking_changes: [Restic uploader path for fs-backup is now deprecated; backups/restores
        still work but emit warnings when restic is selected or used., Node-agent
        configuration ConfigMap name is no longer fixed; you must set `--node-agent-configmap`
        if you use a non-default name., 'Repository maintenance job resource flags
        are effectively replaced by a repository maintenance job configuration ConfigMap
        (flags still work for backwards compatibility, but the ConfigMap takes precedence).',
      The 'Changing PVC selected-node' feature is deprecated and should not be relied
        on going forward.]
  chart_version: 8.7.2
  images: ['docker.io/bitnami/kubectl:1.35', 'velero/velero:v1.15.2']
- version: 1.14.0
  kube: ['1.35', '1.34', '1.33', '1.32', '1.31', '1.30', '1.29', '1.28', '1.27', '1.26',
    '1.25', '1.24', '1.23', '1.22', '1.21', '1.20', '1.19', '1.18']
  requirements: []
  incompatibilities: []
  summary:
    helm_changes: ''
    chart_updates: []
    features: [Repository maintenance for kopia/restic backup repositories is moved
        out of the Velero server pod into separate Kubernetes Jobs to avoid Velero
        server OOMs; resource requests can be configured at install time., 'VolumePolicies
        now support explicit per-volume backup actions (e.g., `fs-backup` vs `snapshot`),
        enabling opt-in/opt-out style control without modifying workloads.', Data
        movement (datamover) pods can be constrained to eligible nodes via a ConfigMap-based
        node selection mechanism., Restore operations now persist VolumeInfo metadata
        for restored volumes and `velero restore describe` surfaces more detail.,
      Restore workflow introduces a new `Finalizing` phase to ensure PV label patching
        and post-restore hooks occur after data movement completes., 'Azure plugin
        adds certificate-based authentication for service principals (in addition
        to secret-based auth), improving phishing resistance and alignment with Azure
        recommendations.']
    breaking_changes: [CSI plugin is now merged into the Velero repo and installed
        by default as an internal plugin; do not install the external CSI plugin via
        `velero install --plugins` anymore., 'Default resource requests/limits for
        the node-agent are removed, making node-agent pods BestEffort QoS unless you
        set resources explicitly.', 'Backup namespace filtering behavior changes:
        if `includedNamespaces`/`excludedNamespaces` are unset but `labelSelector`/`orLabelSelectors`
        are set, only namespaces containing matching resources are included (previously
        all namespaces were included).', 'Because restores patch PVs during the new
        `Finalizing` phase, PVs stuck in `Pending` can cause the restore to end as
        `PartiallyFailed` instead of `Complete` in some cases.']
  chart_version: 7.2.2
  images: ['docker.io/bitnami/kubectl:1.35', 'velero/velero:v1.14.1']
- version: 1.13.0
  kube: ['1.35', '1.34', '1.33', '1.32', '1.31', '1.30', '1.29', '1.28', '1.27', '1.26',
    '1.25', '1.24', '1.23', '1.22', '1.21', '1.20', '1.19', '1.18']
  requirements: []
  incompatibilities: []
  summary:
    helm_changes: ''
    chart_updates: []
    features: ['Resource Modifiers enhanced: in addition to JSON Patch, restores can
        now apply JSON Merge Patch and Strategic Merge Patch via the same ConfigMap
        rules, covering more use cases.', "Node-agent concurrency controls: you can\
        \ cap how many data-movement \u201Cloads\u201D (fs-backups and CSI snapshot\
        \ data mover) run per node globally and/or per-node, helping manage CPU/memory/network\
        \ usage.", 'Kopia performance/behavior knobs: configurable parallel file upload
        and an option to write sparse files during restore for fs-restore and CSI
        snapshot data movement restores.', 'Improved observability: `velero backup
        describe` adds a Backup Volumes section (including CSI snapshot data movement
        details) and backup/restore CRs now include hook execution status fields (hooks
        attempted/failed).', 'New backup VolumeInfo metadata file stored with the
        backup, recording PVC/PV backup method and snapshot/status details to drive
        PV restore behavior; enables downstream tools to summarize volume backup data.',
      'Resilience improvements for CSI snapshot data movement when the Velero server
        pod or node-agent restarts, reducing chances of stuck/abandoned operations.',
      'Azure AD/Workload Identity support expanded to Kopia operations (filesystem
        backup/data mover), not just native snapshots.', 'Dependency updates: Go runtime
        bumped to 1.21.6; Kopia bumped to 0.15.0; AWS SDK for Go moved to v2 for better
        CPU/memory performance.']
    breaking_changes: ['`velero backup describe` output format changed: some existing
        snapshot/fs-backup information moved into the new Backup Volumes section;
        any scripts parsing the old output will need updates.', 'API change: `DataUploadSpec.DataMoverConfig`
        type changed from `*map[string]string` to `map[string]string`; custom tooling/controllers
        must be updated and manifests regenerated if applicable.', 'Installer behavior
        change: informer cache is now enabled by default (the `--disable-informer-cache`
        flag defaults to false). This can increase memory usage and may require raising
        Velero pod memory limits or explicitly disabling the cache if you hit OOMs.']
  chart_version: 6.7.0
  images: ['docker.io/bitnami/kubectl:1.35', 'velero/velero:v1.13.2']
- version: 1.12.0
  kube: ['1.35', '1.34', '1.33', '1.32', '1.31', '1.30', '1.29', '1.28', '1.27', '1.26',
    '1.25', '1.24', '1.23', '1.22', '1.21', '1.20', '1.19', '1.18']
  requirements: []
  incompatibilities: []
  summary:
    helm_changes: ''
    chart_updates: []
    features: ['**CSI Snapshot Data Movement**: Ability to move CSI snapshot data
        into durable backup storage and restore it later/elsewhere (helps on-prem
        durable retention and cross-cloud restores).', '**Resource Modifiers (JSON
        substitutions)**: Apply JSON patches to selected resources during restore
        without writing a custom RestoreItemAction plugin.', '**Multiple VolumeSnapshotClasses
        support**: Choose a specific VolumeSnapshotClass per backup rather than relying
        on driver-name + label heuristics.', '**Restore finalizer cleanup**: `velero
        restore delete` now cleans up associated external/backup-storage data, not
        just the Kubernetes Restore resources.']
    breaking_changes: ['**Default uploader-type changed to Kopia**: installs now default
        to `uploader-type=kopia` instead of `restic`, changing the default filesystem-backup
        path and operational characteristics.', '**CSI snapshot timeout behavior changed**:
        snapshot wait timings are now configurable via `backup.spec.csiSnapshotTimeout`,
        and async readiness waits use the operation timeout (default 4h).', '**Helm
        chart values structure change (v4.0.0)**: BackupStorageLocations (BSL) and
        VolumeSnapshotLocations (VSL) changed from map to slice; update values before
        upgrading to avoid incompatible config.', '**Finalizers can block namespace
        deletion**: deleting the `velero` namespace directly may hang because Restore/DataUpload/DataDownload
        CRs have finalizers; use `velero uninstall` or clear finalizers safely first.']
  chart_version: 5.2.2
  images: ['docker.io/bitnami/kubectl:1.35', 'velero/velero:v1.12.3']
- version: 1.11.0
  kube: ['1.35', '1.34', '1.33', '1.32', '1.31', '1.30', '1.29', '1.28', '1.27', '1.26',
    '1.25', '1.24', '1.23', '1.22', '1.21', '1.20', '1.19', '1.18']
  requirements: []
  incompatibilities: []
  summary: null
  chart_version: 5.0.2
  images: ['docker.io/bitnami/kubectl:1.35', 'velero/velero:v1.11.1']
- version: 1.10.0
  kube: ['1.28', '1.27', '1.26', '1.25', '1.24', '1.23', '1.22', '1.21', '1.20', '1.19',
    '1.18']
  requirements: []
  incompatibilities: []
  summary: null
- version: 1.9.0
  kube: ['1.28', '1.27', '1.26', '1.25', '1.24', '1.23', '1.22', '1.21', '1.20', '1.19',
    '1.18']
  requirements: []
  incompatibilities: []
  summary: null
- version: 1.8.0
  kube: ['1.28', '1.27', '1.26', '1.25', '1.24', '1.23', '1.22', '1.21', '1.20', '1.19',
    '1.18']
  requirements: []
  incompatibilities: []
  summary: null
