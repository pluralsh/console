apiVersion: plural.sh/v1alpha1
kind: Context
spec:
  bundles:
    - name: aws-redis
      repository: redis
    - name: aws-kafka
      repository: kafka
  configuration:
    airflow:
      example: key
    bootstrap:
      another: example
  smtp:
