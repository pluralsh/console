apiVersion: plural.sh/v1alpha1
kind: Context
spec:
  buckets:
  bundles:
    - name: test
      repository: airflow
    - name: aws-redis
      repository: redis
    - name: aws-kafka
      repository: kafka
  configuration:
    airflow:
      example: key
      merged: value
    bootstrap:
      another: example
  domains:
  smtp:
