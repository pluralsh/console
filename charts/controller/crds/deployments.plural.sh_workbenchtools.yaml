---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: workbenchtools.deployments.plural.sh
spec:
  group: deployments.plural.sh
  names:
    kind: WorkbenchTool
    listKind: WorkbenchToolList
    plural: workbenchtools
    singular: workbenchtool
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: ID of the WorkbenchTool in the Console API.
      jsonPath: .status.id
      name: ID
      type: string
    - description: Type of tool.
      jsonPath: .spec.tool
      name: Tool
      type: string
    - description: Flag indicating if the object is read-only
      jsonPath: .status.readonly
      name: READONLY
      type: boolean
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: WorkbenchTool is the Schema for the workbenchtools API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: WorkbenchToolSpec defines the desired state of a WorkbenchTool.
            properties:
              approval:
                description: Whether this tool requires approval before execution.
                type: boolean
              bindings:
                description: Bindings define the read and write access policies for
                  this tool.
                properties:
                  read:
                    description: Read bindings.
                    items:
                      description: Binding used to assign permissions to a resource
                        for a user or a group in the system.
                      properties:
                        UserID:
                          type: string
                        groupID:
                          type: string
                        groupName:
                          type: string
                        id:
                          description: |-
                            ID of a binding.
                            Deprecated.
                            Do not use.
                          type: string
                        userEmail:
                          type: string
                      type: object
                    type: array
                  write:
                    description: Write bindings.
                    items:
                      description: Binding used to assign permissions to a resource
                        for a user or a group in the system.
                      properties:
                        UserID:
                          type: string
                        groupID:
                          type: string
                        groupName:
                          type: string
                        id:
                          description: |-
                            ID of a binding.
                            Deprecated.
                            Do not use.
                          type: string
                        userEmail:
                          type: string
                      type: object
                    type: array
                type: object
              categories:
                description: Categories for the tool.
                items:
                  type: string
                type: array
              cloudConnectionRef:
                description: The cloud connection for this tool (e.g. infrastructure
                  cloud tools).
                properties:
                  apiVersion:
                    description: API version of the referent.
                    type: string
                  fieldPath:
                    description: |-
                      If referring to a piece of an object instead of an entire object, this string
                      should contain a valid JSON/Go field access statement, such as desiredState.manifest.containers[2].
                      For example, if the object reference is to a container within a pod, this would take on a value like:
                      "spec.containers{name}" (where "name" refers to the name of the container that triggered
                      the event) or if no container name is specified "spec.containers[2]" (container with
                      index 2 in this pod). This syntax is chosen only to have some well-defined way of
                      referencing a part of an object.
                    type: string
                  kind:
                    description: |-
                      Kind of the referent.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                    type: string
                  name:
                    description: |-
                      Name of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                  namespace:
                    description: |-
                      Namespace of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
                    type: string
                  resourceVersion:
                    description: |-
                      Specific resourceVersion to which this reference is made, if any.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency
                    type: string
                  uid:
                    description: |-
                      UID of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              configuration:
                description: Tool configuration (e.g. HTTP).
                properties:
                  atlassian:
                    description: Atlassian/jira connection (ticketing).
                    properties:
                      apiTokenSecretRef:
                        description: Reference to a secret key containing the atlassian
                          API token (required if not using service_account).
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      email:
                        description: Atlassian account email (required if not using
                          service_account).
                        type: string
                      serviceAccountSecretRef:
                        description: Reference to a secret key containing the encrypted
                          service account JSON (alternative to api_token + email).
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                    type: object
                  azure:
                    description: Azure monitor connection (metrics).
                    properties:
                      clientId:
                        description: Azure client id.
                        type: string
                      clientSecretSecretRef:
                        description: Reference to a secret key containing the azure
                          client secret.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      prometheusUrl:
                        description: Optional azure managed prometheus url if you
                          wish to use it for metrics.
                        type: string
                      subscriptionId:
                        description: Azure subscription id.
                        type: string
                      tenantId:
                        description: Azure tenant id.
                        type: string
                    required:
                    - clientId
                    - clientSecretSecretRef
                    - subscriptionId
                    - tenantId
                    type: object
                  azureDevops:
                    description: Azure DevOps connection (scm).
                    properties:
                      tokenSecretRef:
                        description: Reference to a secret key containing the Azure
                          DevOps personal access token.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      url:
                        description: Optional REST API root (defaults to https://dev.azure.com).
                        type: string
                    type: object
                  azureFunction:
                    description: Azure Function / Cloud Function configuration.
                    properties:
                      description:
                        description: Description of the function exposed to the agent.
                        type: string
                      identifier:
                        description: Cloud Function identifier.
                        type: string
                      inputSchema:
                        description: JSON schema for the tool input.
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                    required:
                    - description
                    - identifier
                    type: object
                  bitbucket:
                    description: Bitbucket Cloud connection (scm).
                    properties:
                      tokenSecretRef:
                        description: Reference to a secret key containing the Bitbucket
                          app password or access token.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      url:
                        description: Optional Bitbucket Cloud API base URL.
                        type: string
                    type: object
                  bitbucketDatacenter:
                    description: Bitbucket Data Center connection (scm).
                    properties:
                      tokenSecretRef:
                        description: Reference to a secret key containing the HTTP
                          access token or personal access token.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      url:
                        description: Bitbucket Data Center REST API base URL.
                        type: string
                    required:
                    - url
                    type: object
                  cloudRun:
                    description: Google Cloud Run service configuration.
                    properties:
                      description:
                        description: Description of the function exposed to the agent.
                        type: string
                      identifier:
                        description: Cloud Run service identifier.
                        type: string
                      inputSchema:
                        description: JSON schema for the tool input.
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                    required:
                    - description
                    - identifier
                    type: object
                  cloudwatch:
                    description: Cloudwatch connection (metrics, logs).
                    properties:
                      accessKeyIdSecretRef:
                        description: Reference to a secret key containing the optional
                          static AWS access key id.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      externalId:
                        description: Optional external id for assume role.
                        type: string
                      logGroupNames:
                        description: Optional default log groups for CloudWatch Logs
                          Insights.
                        items:
                          type: string
                        type: array
                      region:
                        description: AWS region (e.g. us-east-1).
                        type: string
                      roleArn:
                        description: Optional IAM role ARN to assume.
                        type: string
                      roleSessionName:
                        description: Optional role session name for assume role.
                        type: string
                      secretAccessKeySecretRef:
                        description: Reference to a secret key containing the optional
                          static AWS secret access key.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                    required:
                    - region
                    type: object
                  datadog:
                    description: Datadog connection (metrics, logs).
                    properties:
                      apiKeySecretRef:
                        description: Reference to a secret key containing the datadog
                          API key.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      appKeySecretRef:
                        description: Reference to a secret key containing the datadog
                          application key.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      site:
                        description: Datadog site (e.g. datadoghq.com).
                        type: string
                    type: object
                  docker:
                    description: Docker/OCI registry connection.
                    properties:
                      auth:
                        description: Registry authentication credentials and optional
                          proxy.
                        properties:
                          aws:
                            description: |-
                              Aws specifies AWS-specific authentication for Amazon ECR and other AWS-hosted repositories.
                              Supports both static credentials and IAM role-based authentication for secure access
                              to private repositories hosted in Amazon Web Services.
                            properties:
                              accessKey:
                                description: |-
                                  AccessKey specifies the AWS access key ID for authentication.
                                  When using static credentials, this should be set along with the secret access key.
                                  For enhanced security, consider using IAM roles instead of static credentials.
                                type: string
                              assumeRoleArn:
                                description: |-
                                  AssumeRoleArn specifies an AWS IAM role ARN to assume for repository access.
                                  This enables cross-account access and role-based authentication, providing
                                  enhanced security and flexibility in AWS environments.
                                type: string
                              secretAccessKeySecretKeyRef:
                                description: |-
                                  SecretAccessKeySecretKeyRef references a specific key within a Kubernetes Secret containing the secret access key.
                                  This is the preferred method for storing AWS credentials as it allows multiple
                                  credential sets to be organized within a single secret.
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                              secretAccessKeySecretRef:
                                description: |-
                                  SecretAccessKeySecretRef references a Kubernetes Secret containing the AWS secret access key.
                                  The entire secret content will be used as the secret access key.
                                  This approach is deprecated in favor of SecretAccessKeySecretKeyRef for better secret management.
                                properties:
                                  name:
                                    description: name is unique within a namespace
                                      to reference a secret resource.
                                    type: string
                                  namespace:
                                    description: namespace defines the space within
                                      which the secret name must be unique.
                                    type: string
                                type: object
                                x-kubernetes-map-type: atomic
                            type: object
                          azure:
                            description: |-
                              Azure specifies Azure-specific authentication for Azure Container Registry (ACR).
                              Supports service principal authentication and managed identity for secure access
                              to private repositories hosted in Microsoft Azure.
                            properties:
                              clientId:
                                description: |-
                                  ClientID specifies the Azure service principal client ID.
                                  This is used in conjunction with the client secret to authenticate with Azure services.
                                type: string
                              clientSecretSecretKeyRef:
                                description: |-
                                  ClientSecretSecretKeyRef references a specific key within a Kubernetes Secret containing the client secret.
                                  This is the preferred method for storing Azure credentials as it allows proper
                                  secret organization and key-based access control.
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                              clientSecretSecretRef:
                                description: |-
                                  ClientSecretSecretRef references a Kubernetes Secret containing the Azure service principal client secret.
                                  The entire secret content will be used as the client secret.
                                  This approach is deprecated in favor of ClientSecretSecretKeyRef for better secret management.
                                properties:
                                  name:
                                    description: name is unique within a namespace
                                      to reference a secret resource.
                                    type: string
                                  namespace:
                                    description: namespace defines the space within
                                      which the secret name must be unique.
                                    type: string
                                type: object
                                x-kubernetes-map-type: atomic
                              subscriptionId:
                                description: |-
                                  SubscriptionID specifies the Azure subscription ID.
                                  This identifies the Azure subscription containing the resources
                                  and may be required for certain repository access scenarios.
                                type: string
                              tenantId:
                                description: |-
                                  TenantID specifies the Azure Active Directory tenant ID.
                                  This identifies the Azure AD instance that contains the service principal
                                  and is required for proper authentication scope.
                                type: string
                            type: object
                          basic:
                            description: |-
                              Basic specifies username/password authentication for repositories that support HTTP Basic Auth.
                              Commonly used with private Helm repositories, Harbor registries, and other traditional
                              repository managers that implement standard HTTP authentication.
                            properties:
                              passwordSecretKeyRef:
                                description: |-
                                  PasswordSecretKeyRef references a specific key within a Kubernetes Secret that contains the password.
                                  This is the preferred method for password storage as it allows multiple credentials
                                  to be stored in a single secret with proper key-based access.
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                              passwordSecretRef:
                                description: |-
                                  PasswordSecretRef references a Kubernetes Secret containing the password for Basic authentication.
                                  The entire secret content will be used as the password.
                                  This approach is deprecated in favor of PasswordSecretKeyRef for better secret management.
                                properties:
                                  name:
                                    description: name is unique within a namespace
                                      to reference a secret resource.
                                    type: string
                                  namespace:
                                    description: namespace defines the space within
                                      which the secret name must be unique.
                                    type: string
                                type: object
                                x-kubernetes-map-type: atomic
                              username:
                                description: |-
                                  Username specifies the username for HTTP Basic authentication.
                                  This is typically a user account or service account name configured
                                  in the target repository system.
                                type: string
                            required:
                            - username
                            type: object
                          bearer:
                            description: |-
                              Bearer specifies token-based authentication for repositories that support Bearer tokens.
                              Used with modern container registries and repositories that implement OAuth2 or similar
                              token-based authentication schemes.
                            properties:
                              tokenSecretKeyRef:
                                description: |-
                                  TokenSecretKeyRef references a specific key within a Kubernetes Secret that contains the bearer token.
                                  This is the preferred method for token storage as it allows multiple tokens
                                  to be stored in a single secret with proper key-based access.
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                              tokenSecretRef:
                                description: |-
                                  TokenSecretRef references a Kubernetes Secret containing the bearer token.
                                  The entire secret content will be used as the authentication token.
                                  This approach is deprecated in favor of TokenSecretKeyRef for better secret management.
                                properties:
                                  name:
                                    description: name is unique within a namespace
                                      to reference a secret resource.
                                    type: string
                                  namespace:
                                    description: namespace defines the space within
                                      which the secret name must be unique.
                                    type: string
                                type: object
                                x-kubernetes-map-type: atomic
                            type: object
                          gcp:
                            description: |-
                              Gcp specifies Google Cloud-specific authentication for Google Artifact Registry.
                              Supports service account key authentication for secure access to private
                              repositories hosted in Google Cloud Platform.
                            properties:
                              applicationCredentialsSecretKeyRef:
                                description: |-
                                  ApplicationCredentialsSecretKeyRef references a specific key within a Kubernetes Secret containing the service account JSON.
                                  This is the preferred method for storing GCP credentials as it allows multiple
                                  service account keys to be organized within a single secret with proper access control.
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                              applicationCredentialsSecretRef:
                                description: |-
                                  ApplicationCredentialsSecretRef references a Kubernetes Secret containing the GCP service account key JSON.
                                  The entire secret content will be used as the service account credentials.
                                  This approach is deprecated in favor of ApplicationCredentialsSecretKeyRef for better secret management.
                                properties:
                                  name:
                                    description: name is unique within a namespace
                                      to reference a secret resource.
                                    type: string
                                  namespace:
                                    description: namespace defines the space within
                                      which the secret name must be unique.
                                    type: string
                                type: object
                                x-kubernetes-map-type: atomic
                            type: object
                          proxy:
                            description: Proxy configures an HTTP proxy for requests
                              to this Helm repository or OCI registry.
                            properties:
                              noproxy:
                                description: The comma-separated list of hosts that
                                  should not be proxied, will behave equivalently
                                  to a NOPROXY env var.
                                type: string
                              url:
                                description: The url of your HTTP proxy.
                                type: string
                            required:
                            - url
                            type: object
                        type: object
                      provider:
                        description: 'Registry authentication provider: BASIC, BEARER,
                          AWS, AZURE, or GCP.'
                        enum:
                        - BASIC
                        - BEARER
                        - GCP
                        - AZURE
                        - AWS
                        type: string
                      url:
                        description: Registry host or base URL (defaults to registry-1.docker.io).
                        type: string
                    type: object
                  dynatrace:
                    description: Dynatrace connection (metrics, logs, traces).
                    properties:
                      platformTokenSecretRef:
                        description: Reference to a secret key containing the dynatrace
                          platform token.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      url:
                        description: Dynatrace base URL.
                        type: string
                    required:
                    - platformTokenSecretRef
                    - url
                    type: object
                  elastic:
                    description: Elasticsearch connection (logs).
                    properties:
                      index:
                        description: Elasticsearch index.
                        type: string
                      passwordSecretRef:
                        description: Reference to a secret key containing the basic
                          auth password.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      url:
                        description: Elasticsearch base URL.
                        type: string
                      username:
                        description: Basic auth username.
                        type: string
                    required:
                    - index
                    - url
                    - username
                    type: object
                  exa:
                    description: Exa connection (search).
                    properties:
                      apiKeySecretRef:
                        description: Reference to a secret key containing the exa
                          API key.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                    type: object
                  github:
                    description: GitHub connection (integration).
                    properties:
                      accessTokenSecretRef:
                        description: Reference to a secret key containing an optional
                          GitHub personal access token or fine-grained token.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      appId:
                        description: GitHub App ID (use with installationId and privateKey
                          instead of accessToken).
                        type: string
                      installationId:
                        description: GitHub App installation ID for this organization
                          or account.
                        type: string
                      privateKeySecretRef:
                        description: Reference to a secret key containing the PEM
                          private key for the GitHub App.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      toolset:
                        description: 'Optional native tool subset: issues, pull_requests,
                          repos, security, default/all, or omit for all tools.'
                        type: string
                      url:
                        description: Optional GitHub REST API base URL (defaults to
                          https://api.github.com/; set for GitHub Enterprise Server).
                        type: string
                    type: object
                  gitlab:
                    description: GitLab connection (scm).
                    properties:
                      tokenSecretRef:
                        description: Reference to a secret key containing the GitLab
                          personal access token or project/group token.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      url:
                        description: Optional GitLab API base URL (defaults to https://gitlab.com
                          when omitted).
                        type: string
                    type: object
                  http:
                    description: Http tool configuration.
                    properties:
                      body:
                        description: Request body.
                        type: string
                      function:
                        description: When true, exposes this HTTP tool as a workbench
                          action; execution may require approval when tool approval
                          is enabled.
                        type: boolean
                      headers:
                        description: Request headers.
                        items:
                          description: WorkbenchToolHTTPHeader represents a single
                            HTTP header.
                          properties:
                            name:
                              type: string
                            value:
                              type: string
                          type: object
                        type: array
                      inputSchema:
                        description: JSON schema for the tool input.
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                      method:
                        description: The HTTP method.
                        enum:
                        - GET
                        - POST
                        - PUT
                        - DELETE
                        - PATCH
                        type: string
                      url:
                        description: The request URL.
                        format: uri
                        type: string
                    required:
                    - inputSchema
                    - method
                    - url
                    type: object
                  jaeger:
                    description: Jaeger connection (traces).
                    properties:
                      passwordSecretRef:
                        description: Reference to a secret key containing the basic
                          auth password.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      tokenSecretRef:
                        description: Reference to a secret key containing the bearer
                          token.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      url:
                        description: Jaeger base URL.
                        type: string
                      username:
                        description: Basic auth username.
                        type: string
                    required:
                    - url
                    type: object
                  lambda:
                    description: AWS Lambda function configuration.
                    properties:
                      description:
                        description: Description of the function exposed to the agent.
                        type: string
                      inputSchema:
                        description: JSON schema for the tool input.
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                      lambdaArn:
                        description: AWS Lambda function ARN.
                        type: string
                    required:
                    - description
                    - lambdaArn
                    type: object
                  linear:
                    description: Linear connection (ticketing).
                    properties:
                      accessTokenSecretRef:
                        description: Reference to a secret key containing the linear
                          API access token.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                    type: object
                  loki:
                    description: Loki connection (logs).
                    properties:
                      passwordSecretRef:
                        description: Reference to a secret key containing the basic
                          auth password.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      tenantId:
                        description: Optional tenant id.
                        type: string
                      tokenSecretRef:
                        description: Reference to a secret key containing the bearer
                          token or api key.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      url:
                        description: Loki base URL.
                        type: string
                      username:
                        description: Basic auth username.
                        type: string
                    required:
                    - url
                    type: object
                  opensearch:
                    description: AWS OpenSearch connection (logs).
                    properties:
                      assumeRoleArn:
                        description: Optional IAM role ARN to assume before signing
                          OpenSearch requests.
                        type: string
                      awsAccessKeyIdSecretRef:
                        description: Reference to a secret key containing the AWS
                          access key id for SigV4 authentication.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      awsRegion:
                        description: AWS region for SigV4 authentication.
                        type: string
                      awsSecretAccessKeySecretRef:
                        description: Reference to a secret key containing the AWS
                          secret access key for SigV4 authentication.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      host:
                        description: AWS OpenSearch endpoint.
                        type: string
                      index:
                        description: OpenSearch index.
                        type: string
                      usePodIdentity:
                        description: Whether to use pod identity (IRSA/Workload Identity)
                          for AWS authentication instead of static credentials.
                        type: boolean
                    required:
                    - host
                    - index
                    type: object
                  pagerduty:
                    description: PagerDuty connection (integration).
                    properties:
                      apiTokenSecretRef:
                        description: Reference to a secret key containing the pagerduty
                          REST API key.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                    type: object
                  prometheus:
                    description: Prometheus connection (metrics).
                    properties:
                      awsAccessKeyIdSecretRef:
                        description: Reference to a secret key containing the AWS
                          access key id for SigV4 authentication.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      awsRegion:
                        description: AWS region for SigV4 authentication.
                        type: string
                      awsSecretAccessKeySecretRef:
                        description: Reference to a secret key containing the AWS
                          secret access key for SigV4 authentication.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      awsSigv4:
                        description: Whether to sign requests with AWS SigV4.
                        type: boolean
                      passwordSecretRef:
                        description: Reference to a secret key containing the basic
                          auth password.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      tenantId:
                        description: Optional tenant id (e.g. for Mimir).
                        type: string
                      tokenSecretRef:
                        description: Reference to a secret key containing the bearer
                          token or api key.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      url:
                        description: Prometheus base URL.
                        type: string
                      username:
                        description: Basic auth username.
                        type: string
                    required:
                    - url
                    type: object
                  sentry:
                    description: Sentry connection (error tracking).
                    properties:
                      accessTokenSecretRef:
                        description: Reference to a secret key containing the Sentry
                          user auth token or internal integration token.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      url:
                        description: Optional Sentry API host (defaults to https://sentry.io;
                          set for self-hosted).
                        type: string
                    type: object
                  slack:
                    description: Slack connection (integration).
                    properties:
                      botTokenSecretRef:
                        description: Reference to a secret key containing the slack
                          bot user OAuth token (xoxb-...).
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                    type: object
                  splunk:
                    description: Splunk connection (logs).
                    properties:
                      passwordSecretRef:
                        description: Reference to a secret key containing the basic
                          auth password.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      tokenSecretRef:
                        description: Reference to a secret key containing the bearer
                          token.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      url:
                        description: Splunk base URL.
                        type: string
                      username:
                        description: Basic auth username.
                        type: string
                    required:
                    - url
                    type: object
                  teams:
                    description: Microsoft Teams / Graph connection (integration).
                    properties:
                      clientId:
                        description: Microsoft Entra application (client) id.
                        type: string
                      clientSecretSecretRef:
                        description: Reference to a secret key containing the microsoft
                          entra client secret.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      tenantId:
                        description: Microsoft Entra tenant (directory) id.
                        type: string
                    required:
                    - clientId
                    - clientSecretSecretRef
                    - tenantId
                    type: object
                  tempo:
                    description: Tempo connection (traces).
                    properties:
                      passwordSecretRef:
                        description: Reference to a secret key containing the basic
                          auth password.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      tenantId:
                        description: Optional tenant id.
                        type: string
                      tokenSecretRef:
                        description: Reference to a secret key containing the bearer
                          token or api key.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      url:
                        description: Tempo base URL.
                        type: string
                      username:
                        description: Basic auth username.
                        type: string
                    required:
                    - url
                    type: object
                type: object
              mcpServerRef:
                description: The mcp server for this tool.
                properties:
                  apiVersion:
                    description: API version of the referent.
                    type: string
                  fieldPath:
                    description: |-
                      If referring to a piece of an object instead of an entire object, this string
                      should contain a valid JSON/Go field access statement, such as desiredState.manifest.containers[2].
                      For example, if the object reference is to a container within a pod, this would take on a value like:
                      "spec.containers{name}" (where "name" refers to the name of the container that triggered
                      the event) or if no container name is specified "spec.containers[2]" (container with
                      index 2 in this pod). This syntax is chosen only to have some well-defined way of
                      referencing a part of an object.
                    type: string
                  kind:
                    description: |-
                      Kind of the referent.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                    type: string
                  name:
                    description: |-
                      Name of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                  namespace:
                    description: |-
                      Namespace of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
                    type: string
                  resourceVersion:
                    description: |-
                      Specific resourceVersion to which this reference is made, if any.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency
                    type: string
                  uid:
                    description: |-
                      UID of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              name:
                description: The name of the tool (a-z, 0-9, underscores). If not
                  set, metadata.name is used.
                pattern: ^[a-z0-9_]+$
                type: string
              projectRef:
                description: The project for this tool.
                properties:
                  apiVersion:
                    description: API version of the referent.
                    type: string
                  fieldPath:
                    description: |-
                      If referring to a piece of an object instead of an entire object, this string
                      should contain a valid JSON/Go field access statement, such as desiredState.manifest.containers[2].
                      For example, if the object reference is to a container within a pod, this would take on a value like:
                      "spec.containers{name}" (where "name" refers to the name of the container that triggered
                      the event) or if no container name is specified "spec.containers[2]" (container with
                      index 2 in this pod). This syntax is chosen only to have some well-defined way of
                      referencing a part of an object.
                    type: string
                  kind:
                    description: |-
                      Kind of the referent.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                    type: string
                  name:
                    description: |-
                      Name of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                  namespace:
                    description: |-
                      Namespace of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
                    type: string
                  resourceVersion:
                    description: |-
                      Specific resourceVersion to which this reference is made, if any.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency
                    type: string
                  uid:
                    description: |-
                      UID of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              reconciliation:
                description: Reconciliation parameters for a specific resource.
                properties:
                  driftDetection:
                    default: true
                    description: |-
                      DriftDetection enables drift detection for this resource.
                      It is destined to detect changes made to the related
                      resources that are not referenced with owner ref.
                      Use with Interval to set how often drift detection runs.
                    example: false
                    type: boolean
                  interval:
                    default: 30m
                    description: |-
                      Interval for the drift detection mechanism.
                      It is subject to jitter to avoid the thundering herd problem,
                      it is calculated as a random value between 50% and 150% of the given duration.
                    example: 5m30s
                    type: string
                type: object
              scmConnectionRef:
                description: The SCM connection for this tool (e.g. shared Git provider
                  credentials).
                properties:
                  apiVersion:
                    description: API version of the referent.
                    type: string
                  fieldPath:
                    description: |-
                      If referring to a piece of an object instead of an entire object, this string
                      should contain a valid JSON/Go field access statement, such as desiredState.manifest.containers[2].
                      For example, if the object reference is to a container within a pod, this would take on a value like:
                      "spec.containers{name}" (where "name" refers to the name of the container that triggered
                      the event) or if no container name is specified "spec.containers[2]" (container with
                      index 2 in this pod). This syntax is chosen only to have some well-defined way of
                      referencing a part of an object.
                    type: string
                  kind:
                    description: |-
                      Kind of the referent.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                    type: string
                  name:
                    description: |-
                      Name of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                  namespace:
                    description: |-
                      Namespace of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
                    type: string
                  resourceVersion:
                    description: |-
                      Specific resourceVersion to which this reference is made, if any.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency
                    type: string
                  uid:
                    description: |-
                      UID of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              tool:
                description: The type of tool.
                enum:
                - HTTP
                - ELASTIC
                - DATADOG
                - PROMETHEUS
                - LOKI
                - TEMPO
                - SENTRY
                - MCP
                - LINEAR
                - ATLASSIAN
                - SPLUNK
                - DYNATRACE
                - CLOUDWATCH
                - AZURE
                - CLOUD
                - JAEGER
                - EXA
                - GITHUB
                - SLACK
                - TEAMS
                - GITLAB
                - BITBUCKET
                - BITBUCKET_DATACENTER
                - AZURE_DEVOPS
                - PAGERDUTY
                - OPENSEARCH
                - LAMBDA
                - CLOUD_RUN
                - AZURE_FUNCTION
                - DOCKER
                type: string
            required:
            - tool
            type: object
          status:
            properties:
              conditions:
                description: Represents the observations of a PrAutomation's current
                  state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              id:
                description: ID of the resource in the Console API.
                type: string
              readonly:
                default: false
                description: ReadOnly indicates whether the resource is read-only.
                type: boolean
              sha:
                description: SHA of last applied configuration.
                type: string
            required:
            - readonly
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
