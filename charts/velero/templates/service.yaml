apiVersion: v1
kind: Service
metadata:
  name: s3proxy
spec:
  type: ClusterIP
  ports:
    - name: server
      port: 80
      targetPort: 80
      protocol: TCP
  selector:
    app.kubernetes.io/name: s3proxy
