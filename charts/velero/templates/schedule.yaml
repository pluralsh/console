apiVersion: velero.io/v1
kind: Schedule
metadata:
  name: {{ include "velero.fullname" . }}-default
  labels:
    {{- include "velero.labels" . | nindent 4 }}
spec:
  schedule: {{ .Values.schedule }}
  template:
    ttl: {{ .Values.ttl }}
