velero:
  credentials:
    secretContents:
      cloud: |
        [default]
        aws_access_key_id={{ configuration.identity }}
        aws_secret_access_key={{ configuration.credential }}

s3-proxy:
  s3:
    identity: {{ configuration.identity }}
    credential: {{ configuration.credential }}
  target:
    provider: {{ configuration.provider }}
