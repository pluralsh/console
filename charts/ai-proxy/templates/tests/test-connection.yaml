apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ai-proxy.fullname" . }}-test-connection"
  labels:
    {{- include "ai-proxy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ai-proxy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
