{{- if and (not .Values.opentelemetry.kernelCollector.enabled) .Values.opentelemetry.kernelCollector.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "opentelemetry-collector-kernel-collector.serviceAccountName" .Subcharts.opentelemetry }}
  labels:
    {{- include "opentelemetry-ebpf.labels" . | nindent 4 }}
{{- end -}}
