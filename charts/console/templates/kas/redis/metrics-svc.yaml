{{- /*
Copyright Broadcom, Inc. All Rights Reserved.
SPDX-License-Identifier: APACHE-2.0
*/}}

{{- if and .Values.kas.redis.metrics.enabled .Values.kas.redis.metrics.service.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ printf "%s-metrics" (include "common.names.fullname" .) }}
  namespace: {{ include "common.names.namespace" . | quote }}
  labels: {{- include "common.labels.standard" ( dict "customLabels" .Values.kas.redis.commonLabels "context" $ ) | nindent 4 }}
    app.kubernetes.io/component: metrics
  {{- if or .Values.kas.redis.metrics.service.annotations .Values.kas.redis.commonAnnotations }}
  {{- $annotations := include "common.tplvalues.merge" ( dict "values" ( list .Values.kas.redis.metrics.service.annotations .Values.kas.redis.commonAnnotations ) "context" . ) }}
  annotations: {{- include "common.tplvalues.render" ( dict "value" $annotations "context" $) | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.kas.redis.metrics.service.type }}
  {{- if and .Values.kas.redis.metrics.service.clusterIP (eq .Values.kas.redis.metrics.service.type "ClusterIP") }}
  clusterIP: {{ .Values.kas.redis.metrics.service.clusterIP }}
  {{- end }}
  {{- if eq .Values.kas.redis.metrics.service.type "LoadBalancer" }}
  externalTrafficPolicy: {{ .Values.kas.redis.metrics.service.externalTrafficPolicy }}
  {{- end }}
  {{- if and (eq .Values.kas.redis.metrics.service.type "LoadBalancer") .Values.kas.redis.metrics.service.loadBalancerIP }}
  loadBalancerIP: {{ .Values.kas.redis.metrics.service.loadBalancerIP }}
  {{- end }}
  {{- if and (eq .Values.kas.redis.metrics.service.type "LoadBalancer")  .Values.kas.redis.metrics.service.loadBalancerClass }}
  loadBalancerClass: {{ .Values.kas.redis.metrics.service.loadBalancerClass }}
  {{- end }}
  {{- if and (eq .Values.kas.redis.metrics.service.type "LoadBalancer") .Values.kas.redis.metrics.service.loadBalancerSourceRanges }}
  loadBalancerSourceRanges: {{- toYaml .Values.kas.redis.metrics.service.loadBalancerSourceRanges | nindent 4 }}
  {{- end }}
  ports:
    - name: http-metrics
      port: {{ coalesce .Values.kas.redis.metrics.service.ports.http .Values.kas.redis.metrics.service.port }}
      protocol: TCP
      targetPort: metrics
    {{- if .Values.kas.redis.metrics.service.extraPorts }}
    {{- include "common.tplvalues.render" (dict "value" .Values.kas.redis.metrics.service.extraPorts "context" $) | nindent 4 }}
    {{- end }}
  selector: {{- include "common.labels.matchLabels" ( dict "customLabels" .Values.kas.redis.commonLabels "context" $ ) | nindent 4 }}
{{- end }}
