{{- /*
Copyright Broadcom, Inc. All Rights Reserved.
SPDX-License-Identifier: APACHE-2.0
*/}}

{{- if and .Values.kas.redis.auth.enabled (not .Values.kas.redis.auth.existingSecret) (or .Values.kas.redis.auth.usePasswordFileFromSecret (not .Values.kas.redis.auth.usePasswordFiles)) -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ template "redis.fullname" . }}
  namespace: {{ include "common.names.namespace" . | quote }}
  labels: {{- include "common.labels.standard" ( dict "customLabels" .Values.kas.redis.commonLabels "context" $ ) | nindent 4 }}
  {{- if or .Values.kas.redis.secretAnnotations .Values.kas.redis.commonAnnotations }}
  annotations:
      {{- if .Values.kas.redis.secretAnnotations }}
      {{- include "common.tplvalues.render" ( dict "value" .Values.kas.redis.secretAnnotations "context" $ ) | nindent 4 }}
      {{- end }}
      {{- if .Values.kas.redis.commonAnnotations }}
      {{- include "common.tplvalues.render" ( dict "value" .Values.kas.redis.commonAnnotations "context" $ ) | nindent 4 }}
      {{- end }}
  {{- end }}
type: Opaque
data:
  redis-password: {{ include "redis.password" . | b64enc | quote }}
{{- end -}}
