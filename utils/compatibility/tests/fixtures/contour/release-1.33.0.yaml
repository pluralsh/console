# Image-bearing pod definitions reduced from official v1.33.0 examples/render/contour.yaml
kind: Job
spec:
  template:
    spec:
      containers:
      - name: contour
        image: ghcr.io/projectcontour/contour:v1.33.0
---
kind: Deployment
spec:
  template:
    spec:
      containers:
      - name: contour
        image: ghcr.io/projectcontour/contour:v1.33.0
---
kind: DaemonSet
spec:
  template:
    spec:
      containers:
      - name: shutdown-manager
        image: ghcr.io/projectcontour/contour:v1.33.0
      - name: envoy
        image: docker.io/envoyproxy/envoy:distroless-v1.35.2
      initContainers:
      - name: envoy-initconfig
        image: ghcr.io/projectcontour/contour:v1.33.0
