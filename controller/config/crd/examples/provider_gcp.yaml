---
apiVersion: deployments.plural.sh/v1alpha1
kind: Provider
metadata:
  name: gcp
spec:
  name: gcp
  namespace: operator
  cloud: gcp
  cloudSettings:
    gcp:
      name: credentials
      namespace: operator

